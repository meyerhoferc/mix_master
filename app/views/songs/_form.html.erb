<%= form_for([@artist, @song]) do |f| %>

  <% if @song.errors.any? %>
  <h4><%= pluralize(@song.errors.count, "error") %> prohibited this record from being saved:</h4>
  <ul>
    <% @song.errors.full_messages.each do |error| %>
    <%= error %>
    <% end %>
  </ul>
  <% end %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.submit %>
<% end %>
